{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouvel événement{% endblock %}

{% block body %}


<div class="example-wrapper">
    <h1>Ajouter un nouvel événement</h1>
    {{ form(formCreate) }}
    <twig:BootstrapButton path="{{ path('app_evenement')}}" text="Retour" type="success" />
    {{ dump(request) }}
</div>
{% endblock %}

{% from 'macros/navlink.macro.twig' import nav_link %}
<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Navbar</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
          {{ nav_link ('Accueil', 'app_home') }}
          {{ nav_link ('Tableau de bord', 'app_dashboard') }}
          {{ nav_link ('Voir photo', 'app_picture') }}
          {{ nav_link ('Ajouter photo', 'app_picture_create') }}
          {{ nav_link ('Voir événement', 'app_evenement') }}
          {% if is_granted('IS_AUTHENTICATED_FULLY') %}
          {{ nav_link ('Deconnexion', 'app_logout') }}
          {% else %}
          {{ nav_link ('Inscription', 'app_register') }}
          {{ nav_link ('Connexion', 'app_login') }}
          {% endif %}

        </ul>
      </div>
    </div>
  </nav>
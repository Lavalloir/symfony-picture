{% extends 'base.html.twig' %}

{% block title %}Modifier photo{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Modifier photo</h1>
    {{ form(formCreate) }}
    
    <twig:BootstrapButton path="{{ path('app_picture')}}" text="Retour" type="success" />
    {{ dump(request) }}
</div>
{% endblock %}
